# Dependencies
/node_modules

# Next.js build / static export
/.next
/out
next-env.d.ts
*.tsbuildinfo

# Cloudflare / wrangler
/.wrangler

# Dev server scratch
.dev-server.log
.dev.pid
NUL
/.playwright-mcp

# Large re-downloadable source (145 MB HDX raw; see README for fetch command)
/data-prep/idn_admin2.geojson

# Private, never-publish local artifacts — captures, sessions, audience/PII exports.
# Reorganized under _private/; whole folder ignored. Root patterns kept as a
# backstop for any new capture that lands in the repo root.
/_private/
/pi-session-*.html
/Screenshot*.png
/Screenshot*.jpg
/*_full.txt
/*_feedback*.json
/threads_*.txt
/threads_*.json

# Env / secrets
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db
